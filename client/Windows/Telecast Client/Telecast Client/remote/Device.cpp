#include "Device.h"

bool Device::operator==(const Device& other) const {
	// See what you actually need to compare in the addr structure to make sure that the IP's are different or the same.
	return false;
}